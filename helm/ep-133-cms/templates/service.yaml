apiVersion: v1
kind: Service
metadata:
  name: {{ include "ep-133-cms.fullname" . }}
  labels:
    {{- include "ep-133-cms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ep-133-cms.selectorLabels" . | nindent 4 }}
