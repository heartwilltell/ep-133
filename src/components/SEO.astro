---
// SEO type definitions
// Meta tags are handled in BaseLayout.astro
// This file can be used for page-specific SEO overrides if needed

export interface Props {
  title: string;
  description: string;
  image?: string;
  canonicalUrl?: string;
  type?: 'website' | 'article';
}
---

<!-- This component is a placeholder for SEO type definitions -->
<!-- All meta tags are rendered in BaseLayout.astro -->
